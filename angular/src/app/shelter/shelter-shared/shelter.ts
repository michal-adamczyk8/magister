import {ShelterAddress} from './shelterAddress';

export class Shelter {
  constructor(
    shelterName: string,
    phoneNumber: string,
    email: string,
    nip: string,
    regon: string,
    address: ShelterAddress,
    description: string,
    imagePath: string) {
  }
}
